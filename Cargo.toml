[package]
name = "raspi-labs"
version = "0.1.0"
edition = "2021"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
opt-level = 3

[dependencies]
libm = "0.2"

[build-dependencies]
cc = "1.0"

[features]
default = []
aros-sad = []