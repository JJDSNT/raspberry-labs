/target
kernel8.img
kernel8-be.img
BOOTAA64.EFI

# SD card images (generated)
sd.img
sdcard.img

# Disk images (não versionados)
disks/

# DTBs para QEMU — baixados automaticamente pelo run.sh
# (dtb_qemu/README.txt é rastreado por ser .txt, não .dtb)
dtb_qemu/*.dtb

# Firmware RPi (baixado automaticamente)
firmware/

# Arquivos gerados pelo gen_rom.py
src/emu/c/omega2/memory/aros_main.h
src/emu/c/omega2/memory/aros_ext.h
src/emu/c/omega2/aros_main.h
src/emu/c/omega2/aros_ext.h
src/emu/rom/aros.rom

# ROM duplicata gerada/copiada
src/emu/c/omega2/memory/AROS.ROM

# ROMs proprietárias (Kickstart — não versionadas)
src/emu/rom/Kick*.h
src/emu/rom/kick*.h

# vAmiga (referência externa, não versionado)
referencias/
